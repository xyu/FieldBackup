language: bash

sudo: false

script:
  - bash -c 'shopt -s globstar; shellcheck --shell=ash **/*.sh'
  - bash -c 'find "EnterRouterMode/bin" -maxdepth 1 -type f | xargs -I % bash -c "file % | grep \"32-bit LSB executable\" | grep MIPS"'
